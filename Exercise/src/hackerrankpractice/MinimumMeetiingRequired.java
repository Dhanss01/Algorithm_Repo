package hackerrankpractice;

public class MinimumMeetiingRequired {
	
	/*sort the meeting with the end time
	 * based on end point keep the left pointer and right pointer on starting point
	 * when right Pointer is greater than to the meeting end point  
	 * increment the count by 1, continue and find all the count
	 * count the total number - count there we will get the  
	 * 
	 * 
	 * 
	 * 
	 */

}
