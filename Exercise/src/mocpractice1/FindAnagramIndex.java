package mocpractice1;

import org.junit.Test;

public class FindAnagramIndex {
	
	@Test
	public void test1()
	{
		String s ="hello";
		String target = "ll";
	}
	
	/*init map and start index as 0, end index as 1, init a resultlist
	 *iterate through the input string  till you find the target length and add it in map
	 *add the target values in another map
	 * compare them if both are equal
	 *  --> if yes add the starting index in list
	 *  -->
	 * 
	 * 
	 * 
	 */
	public int findAnagramIndices(String s, String target)
	{
		int start=0;
		
		
		return 1;
	}

}
